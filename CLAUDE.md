# バボカ研究所 / バボカ!! BREAK online — 專案交接文件

這份檔案是給接手的 Claude Code 看的專案背景說明，不是給使用者看的文件。
使用者是 Jolin，台灣人，會用中文（繁體）溝通。這個專案分兩塊：一個靜態的
排球少年 TCG 粉絲網站，跟一個附掛在同個網站上的「バボカ!! BREAK」數位對戰
小遊戲（給 Jolin 自己跟弟弟兩人玩，非公開專案）。

---

## 專案是什麼

- **バボカ研究所**：介紹日本卡牌遊戲《ハイキュー!! バボカ!! BREAK》(排球少年
  TCG) 的非官方粉絲網站，純文字/資料為主，明確聲明不重製官方卡圖（版權考量）。
  已部署在 `jolin8459671.github.io/baboka-lab`。
- **バボカ!! BREAK online**：把同一套規則做成瀏覽器裡的雙人連線對戰遊戲，
  兩人各自用自己的裝置連線（不是同一台裝置輪流玩），透過 Firebase Realtime
  Database 同步。這是這個對話串後半段主要在做的東西。

## 檔案結構

```
/index.html /about.html /cards.html /rules.html /playmat.html   ← 原本的靜態網站頁面，這次沒動內容，只加了導覽連結
/style.css /script.js                                            ← 網站原本的樣式/邏輯
/data/cards.js                                                   ← 卡牌資料庫，網站跟遊戲共用同一份
/play.html /play.js /play.css                                    ← 「本機雙人」雛型版：同一台裝置輪流操作，最早做的版本，邏輯是完整的規則引擎但沒有連線
/play-online.html /online.js /online.css                         ← 「線上對戰」正式版：兩人各自裝置連線，這是目前主力開發的部分
/packs.html /packs.js /packs.css                                  ← 「抽卡包」雛型版：單機功能，不用連線，收藏存 localStorage
/firebase-config.js                                               ← Jolin 已經申請好 Firebase 專案並填好金鑰，不要動這個檔案除非她主動要換專案
```

## 視覺設計語言（沿用既有網站風格，不要另起爐灶）

深色「夜間體育館記分板」風格，CSS 變數定義在 `style.css`（`--ink`
`--floor-2` `--score` `--score-dim` `--whistle` `--chalk` `--chalk-dim`
`--line` `--display`(Anton字體) `--mono`(JetBrains Mono) `--body`）。
卡牌插圖是**原創色塊+姓名字首**的佔位美術（`.cardface` class），刻意不用
官方排球少年美術，因為 Jolin 自己網站上就聲明不重製官方圖，這條線不能破。
之後如果要換真圖，Jolin 會自己準備。

---

## `data/cards.js` 資料現況

目前只收錄兩套起始牌組的卡（不是完整40張正式牌組）：
- D01 烏野起始：日向翔陽、影山飛雄、月島螢、西谷夕、緣下力 + 2張事件卡
- D02 音駒起始：孤爪研磨、黑尾鐵朗、夜久衛輔、灰羽利耶夫、芝山優生 + 2張事件卡
- 另外還有幾張 P02/P03 白鳥澤宣傳卡（西谷PR、牛島若利等），**目前沒有被
  放進任何一個可選牌組池**，online.js 裡的 `POOLS` 只抓 `series` 開頭是
  `D01`/`D02` 的卡。

`online.js` 裡 `COPIES_PER_CARD = 3`：因為每套只有5+2=7種卡，不夠湊成一副
牌，暫時每張複製3份湊出約21張的「測試牌組」。Jolin 之後會補完整的正式
40張牌資料，屆時這個複製邏輯要拔掉，直接照資料庫實際張數建構牌組。

---

## 遊戲規則（已確認過的版本，跟官方規則書核對過）

- Guts(資源) = 疊在角色卡下方的牌，是技能的支付成本，不是手牌
- 角色只能在自己標示的登場區域使用技能
- 阻擋：1人主攔必選，可再加最多2名副攔（同名角色不能同時登場）
- 抽牌時機：只有選擇「接球」才抽牌，選「阻擋」不抽
- **一個 SET（局）= 一次連續的發球到落球的攻防交換**，不是打好幾個回合，
  局末補牌後才會有下一次發球
- 局末補牌：雙方補到6張，輸的一方再從 SET 牌區多補1張（共7張）；SET 牌區
  補不出來 = 直接輸掉整場
- 稀有度 D=起始牌 / P=宣傳卡，跟 N→R→S→頂→秘→極 的補充包序列是分開的兩套系統
- 發球方**不**在發球階段抽牌（Jolin 之前糾正過這點）
- 先取3局(SET)獲勝 = 贏得整場比賽

## `online.js` 遊戲引擎架構

### Firebase 資料模型
- 單一房間節點 `rooms/{5碼房號}`，欄位：`stage`(lobby/presetup/playing/
  interval/matchEnd)、`slots.A`/`slots.B`(present/ready/deckKey/setupDone)、
  `serverKey`、`actingKey`、`phase`、`ball`、`excludeName`、`turnDeadline`、
  `public.A`/`public.B`(deckCount/handCount/setZoneCount/discardCount/
  setsWon/zones)、`lastSetLoser`、`matchWinner`
- `rooms/{code}/log` 是獨立的 push-list（避免多人同時寫入同一個陣列欄位衝突）
- **重要的隱私設計**：每個玩家實際的手牌/牌組/棄牌堆/SET區內容**只存在自己
  瀏覽器的記憶體裡**（`local` 物件），從來不會寫進 Firebase，寫上去的只有
  「數量」跟「已經打出來、正在場上的牌」。這是刻意的，因為資料庫規則是
  雙方都能讀寫全部資料（沒有 Firebase Auth，兩人互信模式），手牌不上傳才
  不會技術上被對方讀到。**代價是對戰中重新整理分頁 = 手牌全部消失**，這是
  已知且跟 Jolin 說明過的限制，不要試圖用 localStorage 存手牌解決，
  Claude 產物的 artifact 環境不支援，而且這是正式網站不是 artifact。

### 單一寫入者原則（避免 race condition）
每個狀態轉換都刻意設計成只有一方會寫入，避免雙方同時寫同一個欄位衝突：
- `lobby → presetup`、`presetup → playing`（擲硬幣）、`interval → playing`
  這三個轉換固定由 **slot A（房主）**負責觸發
- `playing` 階段內，只有 `actingKey` 對應的那個玩家會寫入
- 每回合結束觸發 `endSet()` 的，一定是剛好在那個當下持有 `actingKey` 的
  那一方（自然只有一人會呼叫）

### 已修過、之後不要重蹈覆轍的 bug 類型
1. **Firebase 會把值是 `null` 的欄位直接刪掉**，一個物件底下所有欄位都是
   `null` 時，整個物件節點會消失（不是變成 `{}`，是完全不存在）。之前
   `zones: {serve:null,...}` 跟 `excludeName: {toss:null, attack:null}`
   兩處都因此在讀取時整個物件是 `undefined`，炸出 `Cannot read properties
   of undefined`。修法：讀取時一律 `pub.zones || {}` 防呆，不要假設物件一
   定存在。
2. **在 `db.update()` 呼叫之後，不要直接讀取剛剛寫入的同一個欄位**，因為
   `room` 是同一個被 mutate 的物件參照，onValue 的 callback 有時候會在
   `db.update()` 同步回傳之前就先觸發（尤其是自己寫入自己訂閱的路徑時）。
   正確做法：要用的值先存到本地 const，再呼叫 `db.update()`。已經在
   `endSet()` 跟局末轉場那兩處修過，新增邏輯時要注意同樣的模式。

### 測試方式（沒有真的瀏覽器可以用，只能測邏輯不能測畫面）
這次開發全程用 **jsdom** 模擬雙客戶端對戰（不是 Playwright，jsdom 不會真
的算 CSS/排版/3D transform，`getBoundingClientRect()` 永遠回傳0）。做法是
寫一個貼近 Firebase 語意（含 null 刪欄位那個行為）的 mock 資料庫，開兩個
jsdom window 共用同一份 mock store，用隨機點擊跑幾十到上百場完整對局，
攔截 `window.addEventListener('error', ...)` 抓未捕捉例外。這個方法能抓
邏輯錯誤、卡死、undefined 崩潰，**完全不能驗證視覺效果**（3D透視角度好不
好看、扇形手牌會不會重疊、動畫順不順），這塊過去都是請 Jolin 自己截圖
回饋。

**如果 Claude Code 這邊已經裝了 Playwright，這是一個很大的能力提升**：
可以直接開真瀏覽器把 `play-online.html` 用兩個 context 分別當玩家一玩家
二跑起來，截圖檢查 3D 透視、動畫、版面，不用每次都等 Jolin 手動測試回報。
強烈建議之後的視覺類調整都用 Playwright 截圖自己先看過一輪再交給 Jolin。

---

## 目前功能完成度

### 已完成、測過穩定的
- 建房/加入房（5碼房號）、選牌組、雙方 ready 後自動擲硬幣開局
- 完整規則引擎：發球 → 接球/阻擋(阻擋可1主+2副) → 舉球 → 攻擊，點數比對、
  落球判定、局末補牌、SET區耗盡判整場輸、先3局勝出
- 15秒回合倒數（純顯示，時間到不會自動做任何事，Jolin 明確說先不要做
  自動判落球）
- 手動「放棄」鈕，隨時可以宣告落球
- **技能自動化，涵蓋7張角色**（日向翔陽、影山飛雄、西谷夕、孤爪研磨、
  芝山優生用「場上資源」付費；緣下力、夜久衛輔用「丟1張手牌」付費，丟
  棄目標自動選手牌最後一張，不讓玩家挑）。點手牌時如果符合自動化規則會
  跳確認面板（用技能/不用技能直接出牌/取消重選），資源計數
  `local.zonePile` 每局(SET)開始時歸零，靠 `SKILL_RULES` 這張表定義
  cost/effect
- 視覺：3D透視對戰桌面（`.tablescene`/`.tablesurface`，`rotateX`約50度，
  卡片內容用反向旋轉抵銷傾斜保持可讀）、扇形手牌展開、卡面箔面光澤掃過
  動畫、自己出牌時卡片會用 FLIP 技巧真的從手牌位置飛到場上對應區域
  （對方出牌因為看不到對方手牌實際位置，只能用普通的浮現動畫）

### 明確還沒做、Jolin 知情且接受先擱置的
- **月島螢、黑尾鐵朗、灰羽利耶夫**這3張的技能沒自動化（分別涉及阻擋階段
  多選流程結算、影響局末結算、同時登場兩個區域，結構太複雜這次先跳過，
  還是用手動加減值輸入框處理）
- **事件卡完全不能出牌**——4張起始事件卡（排球是!!!、Broad攻擊、不是說了
  手要往前伸嗎、用物理攻擊揍下去就好了吧）目前整個引擎從最初的 `play.js`
  雛型版開始就沒有把事件卡接進任何出牌流程，只能在手牌裡看到、看技能文字，
  完全不能用。這是比較早期就存在的缺口，不是這次新增的問題
- P02/P03 白鳥澤宣傳卡不在任何可選牌組池裡
- 15秒倒數時間到之後的自動判落球——Jolin 明確要求先不要做
- 15秒倒數目前是**每個 client 自己在跑 `setInterval` 純視覺倒數**，沒有
  跟伺服器時間校正，理論上兩邊看到的秒數可能有些微落差，這不影響遊戲
  結果判定（因為沒有自動判落球），只是顯示用

### 已知但刻意接受的限制（不是bug，是取捨）
- 對戰中重新整理分頁 = 手牌消失（見上面「隱私設計」那段的原因）
- Firebase 規則是雙方全開讀寫（`.read:true, .write:true`），沒有帳號驗證，
  純粹兩人互信模式，不是防作弊等級的設計
- 卡牌插圖是原創色塊佔位圖，不是官方美術（版權考量，Jolin 已經接受這點）

---

## `packs.js` 抽卡包（雛型版，這次新做的，跟連線對戰是分開的功能）

- 不用 Firebase，單機功能，收藏紀錄用 `localStorage`（key: `baboka_collection_v1`）
  存在使用者自己瀏覽器裡，換裝置/清瀏覽器資料就不見了，這是已知取捨。
  **這裡用 localStorage 沒問題**，因為 `packs.html` 是真的部署出去的網站頁面，
  不是 Claude.ai 的 artifact 沙盒——artifact 環境才禁止用 localStorage，
  這條限制不適用於這個專案的其他部分。
- **設計決定**：卡包只會抽 `rarity !== 'Deck'` 的卡（也就是起始套牌本身的
  牌不會出現在卡包裡，邏輯上跟正式 TCG 一樣，起始套牌卡不會混進補充包）。
  目前這個池子只有5張（西谷夕PR、牛島若利、球場裡到處都是情報、那你到底
  在做什麼、給我們的攻擊手讓路），因為 `data/cards.js` 裡還沒有真的
  N/R/S/頂/秘/極 補充包卡。**Jolin 之後補完整補充包資料後，這支程式完全
  不用改，池子會自動變大**——這是她明確要求的設計目標，不要因為「反正
  以後要改」就先寫死或跳過這個泛用性。
- 稀有度加權機率表 `RARITY_WEIGHT`（N/R/S/頂/秘/極）是我先抓的參考值
  （N 60% R 27% S 10% 頂 2% 秘 0.7% 極 0.3%），只對「實際存在於卡池裡的
  稀有度」做重新正規化。這組數字沒有跟 Jolin 討論過細節，只是先求邏輯
  跑得動，之後如果她想調整手感（想多爽一點還是多稀有一點）要記得問她。
- 每包固定 `PACK_SIZE = 5` 張，開包畫面先蓋牌、可以一張一張點開或按
  「全部翻開」，稀有卡（S以上）翻開時有一圈光暈動畫。
- 圖鑑畫面分兩區塊：卡包收藏（鎖住的卡顯示「？」剪影）跟起始套牌（一定
  持有，不用抽，純展示）。
- 跑過100次開包+瀏覽圖鑑的 jsdom 模擬，確認不會報錯、5張測試卡池最後
  100%收藏到，邏輯沒問題。跟對戰引擎一樣，**視覺效果（翻牌動畫順不順、
  光暈好不好看）沒有測過，只驗證了邏輯**。

---

## 跟 Jolin 溝通時的注意事項

- 她習慣繁體中文（台灣用語），回覆偏好條理分明、直奔重點、善用列點加粗
- 解釋複雜概念習慣用比喻或具體範例
- 她會自己截圖回報畫面問題，之前這樣來回調過3D透視、扇形手牌、光澤動畫
  等好幾輪，這是正常的迭代方式，不代表哪次做錯了
- 她原本用 Claude.ai 網頁版聊天做這個專案到目前為止，現在要換來
  Claude Code 繼續做，主要原因是想用 Playwright 做視覺測試（Claude.ai
  聊天的沙盒環境網路白名單擋掉了 Playwright 瀏覽器執行檔下載，Claude Code
  跑在她自己電腦上沒有這個限制）
- 這份文件之前的完整開發過程（含使用者一開始怎麼問「排球少年有沒有App」
  一路聊到做出這個東西的來龍去脈）她自己有聊天記錄，不需要在 Claude Code
  裡重新交代原始需求脈絡，這份文件已經是濃縮過的現況總結
