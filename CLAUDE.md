# バボカ研究所 / バボカ!! BREAK online — 專案交接文件

這份檔案是給接手的 Claude Code 看的專案背景說明，不是給使用者看的文件。
使用者是 Jolin，台灣人，會用中文（繁體）溝通。這個專案分三塊：一個靜態的
排球少年 TCG 粉絲網站、一個附掛在同個網站上的「バボカ!! BREAK」數位對戰
小遊戲（給 Jolin 自己跟弟弟兩人玩，非公開專案），以及一個抽卡包雛型（單機
收藏養成，Jolin 自己在玩）。

---

## 專案是什麼

- **バボカ研究所**：介紹日本卡牌遊戲《ハイキュー!! バボカ!! BREAK》(排球少年
  TCG) 的非官方粉絲網站，純文字/資料為主，明確聲明不重製官方卡圖（版權考量，
  但見下方「卡牌插圖」一節——這條線後來有調整）。已部署在
  `jolin8459671.github.io/baboka-lab`。
- **バボカ!! BREAK online**：把同一套規則做成瀏覽器裡的雙人連線對戰遊戲，
  兩人各自用自己的裝置連線（不是同一台裝置輪流玩），透過 Firebase Realtime
  Database 同步。畫面是滿版沉浸式對戰墊（詳見下方章節），不是傳統網頁排版。
- **抽卡包**：Pokémon TCG Pocket 風格的雛型單機收藏養成，模擬拆包、翻牌，
  收藏紀錄存在 Firebase（原本是 localStorage，後來 Jolin 要求換成資料庫，
  因為她想留著收藏繼續開發，不想因為清瀏覽器資料就重置）。

## 檔案結構

```
/index.html /about.html /cards.html /rules.html /playmat.html   ← 原本的靜態網站頁面
/style.css /script.js                                            ← 網站原本的樣式/邏輯（含 cards.html 的卡牌資料庫搜尋/篩選）
/data/cards.js                                                   ← 卡牌資料庫，網站/線上對戰/抽卡包共用同一份
/play-online.html /online.js /online.css                         ← 「線上對戰」正式版：兩人各自裝置連線，滿版對戰墊 UI（唯一的對戰入口，原本還有一個「本機雙人」同裝置輪流版本〔play.html/play.js/play.css〕，Jolin 覺得有線上對戰就夠了，已經整個刪掉，不要再重建）
/packs.html /packs.js /packs.css                                 ← 「抽卡包」：單機互動，收藏存 Firebase
/firebase-config.js                                               ← Jolin 已經申請好 Firebase 專案並填好金鑰，不要動這個檔案除非她主動要換專案
/assets/cards/*.webp                                              ← 卡牌插圖（Jolin 自己準備的圖，非官方美術經處理過的版本，見下方說明）
/assets/*_playmat*.{svg,png}                                      ← 桌墊下載頁用的原創桌墊設計圖，其中 single_playmat_print.png 也被線上對戰拿來當滿版背景
/scripts/*.js /package.json                                       ← Claude Code 這邊建的 Playwright 測試工具鏈，見下方「測試方式」章節
```

## 視覺設計語言

深色「夜間體育館記分板」風格，CSS 變數定義在 `style.css`（`--ink`
`--floor-2` `--score` `--score-dim` `--whistle` `--chalk` `--chalk-dim`
`--line` `--display`(Anton字體) `--mono`(JetBrains Mono) `--body`）。網站本體
（首頁、規則、卡牌資料庫等頁面）跟抽卡包頁面都維持這個風格。

**卡牌插圖**：`data/cards.js` 現在**每一張卡都有 `image` 欄位**指向
`assets/cards/*.webp`（Jolin 自己準備的圖片，不是直接用官方原圖，符合網站
版權立場）。兩個渲染卡面的地方都支援「有 `image` 就疊一張 `<img>` 蓋掉色塊
背景，沒有就照舊顯示原創色塊+姓名字首佔位美術」：`online.js` 的
`cardFaceHTML`、`packs.js` 的 `pcardHTML`。`cards.html` 的卡牌資料庫頁
（`script.js`）目前**沒有**接這個圖片邏輯，只顯示文字資訊，這是刻意維持的
範圍，不是漏掉。

---

## `data/cards.js` 資料現況

**36 筆卡片資料**，對應 `assets/cards/` 裡 36 個圖檔，每張都已經有圖：

- **D01 烏野起始**：14 筆資料、實際牌組 **40 張**。10 名角色 + 2 張事件卡，
  其中日向翔陽、影山飛雄兩張王牌各有一張「DP版」（同卡號、多一個
  `variant: "DP"` 欄位、獨立的 `-DP.webp` 圖檔），配牌張數是照官方
  スターターデッキ的収録內容圖核對過的正確張數（不是隨便複製湊數）：
  日向翔陽 D×3+DP×1、影山飛雄 D×3+DP×1、月島螢×2、山口忠×2、西谷夕×2、
  田中龍之介×4、緣下力×2、澤村大地×4、菅原孝支×4、東峰旭×4、
  兩張事件卡（排球是!!!／Broad攻擊）各×4。
- **D02 音駒起始**：14 筆資料、實際牌組 **40 張**，配置邏輯跟結構完全對稱
  於烏野——孤爪研磨、黑尾鐵朗是雙王牌，各 D×3+DP×1；夜久衛輔、海信行、
  芝山優生、手白球彦各×2；灰羽利耶夫、山本猛虎、福永招平、犬岡走、兩張
  事件卡各×4。同樣是照官方收錄內容圖核對過的正確張數。
- **P02/P03 白鳥澤宣傳卡**：8 張（西谷夕PR、牛島若利、成田一仁、
  澤村・黑尾雙面卡、灰羽アリサ + 3張事件卡），**不在**任何起始牌組池裡
  （`POOLS` 只抓 `series` 開頭是 `D01`/`D02` 的卡），但會出現在抽卡包的
  booster pool（`rarity !== 'Deck'` 的都會進去）。

**`copies` / `variant` 欄位是這次新加的資料模型**：每張卡可以宣告
`copies: N` 代表正式牌組裡有幾張；`buildDeck()`（在 `online.js` 裡）
現在會優先讀這個值，**沒填才 fallback 用**
`COPIES_PER_CARD = 3`（目前資料庫每張都填了，這個常數只是防呆備援，
用不到）。`variant: "DP"` 標記同一張卡的平行/閃卡版本——**卡號
(`code`) 故意跟原版相同**，這是照官方卡片檢索網站（takaratomy.co.jp）
自己的資料模型做的：D版跟DP版共用同一個卡號，只用另一個欄位區分，不是
Claude 發明的編號規則。

---

## 遊戲規則（已確認過的版本，跟官方規則書核對過）

- Guts(資源) = 疊在角色卡下方的牌，是技能的支付成本，不是手牌
- 角色只能在自己標示的登場區域使用技能
- 阻擋：1人主攔必選，可再加最多2名副攔（同名角色不能同時登場）
- 抽牌時機：只有選擇「接球」才抽牌，選「阻擋」不抽
- **一個 SET（局）= 一次連續的發球到落球的攻防交換**，不是打好幾個回合，
  局末補牌後才會有下一次發球
- 局末補牌：雙方補到6張，輸的一方再從 SET 牌區多補1張（共7張）；SET 牌區
  補不出來 = 直接輸掉整場
- 稀有度 D=起始牌 / P=宣傳卡，跟 N→R→S→頂→秘→極 的補充包序列是分開的兩套系統
- 發球方**不**在發球階段抽牌
- 先取3局(SET)獲勝 = 贏得整場比賽

---

## `online.js` 線上對戰引擎架構

### Firebase 資料模型
- 單一房間節點 `rooms/{5碼房號}`，欄位：`stage`(lobby/presetup/playing/
  interval/matchEnd)、`slots.A`/`slots.B`(present/ready/deckKey/setupDone)、
  `serverKey`、`actingKey`、`phase`、`ball`、`excludeName`、`turnDeadline`、
  `public.A`/`public.B`(deckCount/handCount/setZoneCount/discardCount/
  setsWon/zones)、`lastSetLoser`、`matchWinner`
- `rooms/{code}/log` 是獨立的 push-list（避免多人同時寫入同一個陣列欄位衝突）
- **重要的隱私設計**：每個玩家實際的手牌/牌組/棄牌堆/SET區內容**只存在自己
  瀏覽器的記憶體裡**（`local` 物件），從來不會寫進 Firebase，寫上去的只有
  「數量」跟「已經打出來、正在場上的牌」。這是刻意的，因為資料庫規則是
  雙方都能讀寫全部資料（沒有 Firebase Auth，兩人互信模式），手牌不上傳才
  不會技術上被對方讀到。**代價是對戰中重新整理分頁 = 手牌全部消失**，這是
  已知且跟 Jolin 說明過的限制，不要試圖用 localStorage 存手牌解決。

### 單一寫入者原則（避免 race condition）
每個狀態轉換都刻意設計成只有一方會寫入，避免雙方同時寫同一個欄位衝突：
- `lobby → presetup`、`presetup → playing`（擲硬幣）、`interval → playing`
  這三個轉換固定由 **slot A（房主）**負責觸發
- `playing` 階段內，只有 `actingKey` 對應的那個玩家會寫入
- 每回合結束觸發 `endSet()` 的，一定是剛好在那個當下持有 `actingKey` 的
  那一方（自然只有一人會呼叫）

### 已修過、之後不要重蹈覆轍的 bug 類型
1. **Firebase 會把值是 `null` 的欄位直接刪掉**，一個物件底下所有欄位都是
   `null` 時，整個物件節點會消失（不是變成 `{}`，是完全不存在）。讀取時
   一律 `pub.zones || {}` 防呆，不要假設物件一定存在。
2. **在 `db.update()` 呼叫之後，不要直接讀取剛剛寫入的同一個欄位**，因為
   `room` 是同一個被 mutate 的物件參照，onValue 的 callback 有時候會在
   `db.update()` 同步回傳之前就先觸發。正確做法：要用的值先存到本地
   const，再呼叫 `db.update()`。
3. **物件裡塞 `undefined`（不是 `null`）會讓 Firebase SDK 直接同步拋例外**，
   跟 `null` 的「靜默刪欄位」是兩種完全不同的行為。之前
   `writeZone()` 寫 `skill: card.skill` 時，若某張卡忘記填 `skill` 欄位
   （值是 `undefined`），出牌當下就會整個報錯卡死那個回合。教訓：
   `data/cards.js` 裡**每張角色卡都必須有非空的 `skill` 字串**，沒有特殊
   技能就填 `"無特殊技能。"`，不要整個省略這個欄位。

### 滿版沉浸式對戰墊 UI（取代了原本的 CSS 3D透視桌面）
`play-online.html` 進入 `playing` 畫面時，`render()` 會幫 `<body>` 加上
`fs-battle` class，CSS（`online.css` 的 `body.fs-battle` 規則）藏起網站
導覽列/大標題，`.matbattle` 用 `position:fixed;inset:0` 撐滿整個螢幕：

- 背景圖直接用桌墊下載頁的 `assets/single_playmat_print.png`（單人版原創
  桌墊設計），比照實體對戰「兩張單人墊阻擋區相對擺放、中間留球網」的物理
  配置：對手那張整個 `rotate(180deg)` 接在畫面上緣，我方正向接在下緣，
  中間夾一條顯示來球資訊的窄帶（`.matbattle__netgap`）
- 每個出牌區塊（發球/接球/阻擋/舉球/攻擊/牌組/局數/棄牌）都用**絕對定位的
  百分比座標**精準疊在桌墊印刷圖對應的格子上（座標是照
  `assets/single_playmat.svg` 裡的 SVG 座標換算的，不是目測），對手那排因
  為整層轉了180度，子元素的文字/卡面另外用 `.matlayer--opp .zone__face
  {transform:rotate(180deg)}` 轉正
- **`layoutMatBattle()`**：對戰墊是固定 12:7 比例的圖，兩張疊起來的高度
  常常會超過螢幕，這個函式會量 `.matbattle__mat` 容器實際可用的寬/高，
  取「寬度撐滿」跟「高度撐滿」兩種算法中比較小的那個，用 px 精準設定每張
  墊子的寬高。**不要改回純 CSS `aspect-ratio` 硬套**，之前這樣做過，
  兩張墊子疊起來會比螢幕高很多，畫面預設捲動位置卡在正中間（阻擋區那條
  縫）看起來像被放大好幾倍，是真實發生過的 bug。有掛 `resize` 監聽，
  換視窗大小會重新計算。
- 手牌/出牌操作區、對戰紀錄改成浮在墊子上方的半透明面板
  （`.matbattle__dock` 固定在底部、`.matbattle__logdrawer` 是右側抽屜，
  預設收起，點 HUD 上的「紀錄」按鈕才展開）

### 手牌永遠完整顯示
`actionZoneHTML()` 現在**永遠列出整副手牌**，不符合目前階段條件的牌用
`.handcard.disabled`（灰階、`onclick` 是空字串）呈現，而不是像早期版本那樣
直接把不能出的牌從陣列裡濾掉、畫面上完全看不到。這是 Jolin 明確要求的
（「這樣玩家會不知道自己有哪些手牌」），改動邏輯在 `actionWrap()`。

### 測試方式：Playwright 已經裝好、能用真瀏覽器測
這一點**跟很久以前的舊版說明完全相反**：換到 Claude Code 之後裝了
`@playwright/test`（見 `package.json`），可以真的開 Chromium 幫 Jolin
截圖看畫面，不用再只靠 jsdom 模擬邏輯。工具鏈都在 `scripts/`：

- `scripts/serve.js`：最小靜態伺服器，`node scripts/serve.js`（或
  `npm run serve`）把整個網站架在 `http://localhost:4173`
- `scripts/screenshot.js`：單頁截圖小工具，
  `node scripts/screenshot.js <頁面路徑> <輸出檔名> [寬] [高]`
- `scripts/playmat-flow.js`：**線上對戰完整雙人流程**（開房→加入→ready→
  presetup→擲硬幣→發球→應對），開兩個 browser context 分別當玩家一/玩家
  二，走真的 Firebase（不是 mock），每個關鍵畫面都截圖存到指定資料夾，
  也會印出 console error / pageerror。每次改線上對戰相關的東西都應該重跑
  這支確認沒壞
- `scripts/packs-flow.js`：抽卡包完整流程（選包→撕包手勢→翻牌手勢→查看
  圖鑑），用合成的 `PointerEvent` 模擬滑動手勢
- `scripts/site-check.js`：全站 7 個頁面逐一載入，抓 console error /
  pageerror / 破圖(404 image)，改完東西後的第一道防線
- 跑之前記得先啟動 `scripts/serve.js`（背景執行），這些腳本都是打
  `localhost:4173`，不是開 `file://`

**流程仍然無法自動判斷「畫面好不好看」**（版面美感、動畫流暢度），這塊
還是需要 Jolin 自己看截圖回饋，但至少「有沒有 console error、卡死、
座標對不對、圖有沒有破」這些都可以自己先跑過一輪再交給她看。

---

## `packs.js` 抽卡包

- **收藏紀錄存 Firebase Realtime Database**（`packsCollection` 路徑，跟
  `rooms/` 分開，不會互相影響），不是 localStorage——這是 Jolin 主動要求
  換的，原因是她想留著抽到的卡繼續開發，不想因為換裝置/清瀏覽器資料就
  重置。轉接層寫法跟 `online.js` 的 `getDB()` 同一套（含 `window.__mockDB`
  測試 hook）。畫面上有處理「連線中…」跟資料庫連不上的錯誤狀態
  （`.errorbox`，樣式定義在 `packs.css`，跟 `online.css` 那份是各自獨立的
  class，兩邊都要顧到）。
- **設計決定**：卡包只會抽 `rarity !== 'Deck'` 的卡（起始套牌本身的牌不會
  出現在卡包裡），目前這個池子是 P02/P03 那 8 張宣傳卡。Jolin 之後補完整
  N/R/S/頂/秘/極 補充包資料後，這支程式完全不用改，池子會自動變大。
- 稀有度加權機率表 `RARITY_WEIGHT`（N 60% R 27% S 10% 頂 2% 秘 0.7%
  極 0.3%）只對「實際存在於卡池裡的稀有度」做重新正規化，這組數字沒有跟
  Jolin 討論過細節，之後如果她想調整手感要記得問她。
- 每包固定 `PACK_SIZE = 5` 張。互動流程：**選包畫面**（3個外觀一樣的包給
  你選，純儀式感）→ **撕包畫面**（Pointer Events 做「按住/手指按著往上滑」
  的手勢，滑超過70px觸發撕開，單純點一下也算數）→ **翻牌堆疊**（最上面
  那張左右滑走觸發翻開+進入收藏，滑超過70px或點一下都算數）。手勢邏輯用
  事件代理掛在 `#app` 上（不是掛個別卡片，因為每次 `render()` 都整個
  innerHTML 重繪，個別綁定的監聽器會失效）。
- **卡背圖**：`assets/cards/back.webp`（Jolin 提供），用在兩個地方——翻牌
  堆疊裡背面朝上還沒翻開的卡（`pcardHTML(card,{faceDown:true})`），跟圖鑑
  裡「未取得」鎖住的卡（`opts.locked`）。這兩個狀態現在都疊圖片而不是純
  文字「？」，小小的「？」文字只留在稀有度角標當標記。
- **點卡面放大看圖**：只要 `pcardHTML` 判斷這張卡「有真的顯示圖片」（不是
  鎖住、也不是背面朝上），就會在卡片上加 `data-fullimg`/`.zoomable`，
  事件代理在 `#app` 上抓 click 開一個全螢幕 `.imglightbox` 疊層顯示放大圖
  （點背景或按 Esc 關閉）。這個邏輯故意跟撕包/翻牌的拖曳手勢（pointerdown
  系列事件）分開判斷，不會互相干擾。
- 稀有卡（S以上）翻開時有一圈光暈動畫。圖鑑畫面分兩區塊：卡包收藏（鎖住
  的卡顯示卡背圖）跟起始套牌（一定持有，不用抽，純展示）。

---

## 目前功能完成度

### 已完成、測過穩定的
- 建房/加入房（5碼房號）、選牌組、雙方 ready 後自動擲硬幣開局
- 完整規則引擎：發球 → 接球/阻擋(阻擋可1主+2副) → 舉球 → 攻擊，點數比對、
  落球判定、局末補牌、SET區耗盡判整場輸、先3局勝出
- 15秒回合倒數（純顯示，時間到不會自動做任何事，Jolin 明確說先不要做
  自動判落球；每個 client 自己跑 `setInterval`，沒有跟伺服器時間校正，
  兩邊看到的秒數可能有些微落差，不影響判定只影響顯示）
- 手動「放棄」鈕，隨時可以宣告落球
- **技能自動化，涵蓋7張角色**（日向翔陽、影山飛雄、西谷夕、孤爪研磨、
  芝山優生用「場上資源」付費；緣下力、夜久衛輔用「丟1張手牌」付費，丟
  棄目標自動選手牌最後一張，不讓玩家挑）。點手牌時如果符合自動化規則會
  跳確認面板（用技能/不用技能直接出牌/取消重選）
- 手牌永遠完整顯示，不符合當下階段的牌灰階顯示但不隱藏（見上方章節）
- 滿版沉浸式對戰墊 UI（見上方章節），出牌動畫（FLIP 技巧從手牌位置飛到
  場上對應區域，`runFlipAnimation`）、扇形手牌展開、卡面箔面光澤掃過動畫
- **卡牌圖片支援**：36 張卡全部都有圖，三處卡面渲染邏輯都已接上（見上方
  「視覺設計語言」章節）
- **烏野/音駒兩副起始牌組都精準對照官方収録內容重建為 40 張**（見上方
  `data/cards.js` 章節），不再是複製湊數的測試牌組
- 抽卡包完整雛型：選包/撕包/翻牌手勢、稀有度加權、收藏存 Firebase、圖鑑、
  卡背圖、點圖放大
- Playwright 測試工具鏈（見上方「測試方式」章節）

### 明確還沒做、Jolin 知情且接受先擱置的
- **月島螢、黑尾鐵朗、灰羽利耶夫**這3張的技能沒自動化（分別涉及阻擋階段
  多選流程結算、影響局末結算、同時登場兩個區域，結構太複雜這次先跳過，
  還是用手動加減值輸入框處理）
- **事件卡完全不能出牌**——所有事件卡目前整個 `online.js` 引擎都沒有把
  事件卡接進任何出牌流程，只能在手牌裡看到、看技能文字，完全不能用
- 15秒倒數時間到之後的自動判落球——Jolin 明確要求先不要做
- 補充包（N/R/S/頂/秘/極）資料還沒補，`packs.js` 的卡池目前只有 8 張
  P02/P03 宣傳卡

### 已知但刻意接受的限制（不是bug，是取捨）
- 對戰中重新整理分頁 = 手牌消失（見「隱私設計」段落的原因）
- Firebase 規則是雙方全開讀寫（`.read:true, .write:true`），沒有帳號驗證，
  純粹兩人互信模式，不是防作弊等級的設計
- 卡牌插圖是 Jolin 自己準備的圖，不是直接用官方原圖

---

## 跟 Jolin 溝通時的注意事項

- 她習慣繁體中文（台灣用語），回覆偏好條理分明、直奔重點、善用列點加粗
- 解釋複雜概念習慣用比喻或具體範例
- 她會自己截圖回報畫面問題，這是正常的迭代方式，不代表哪次做錯了；她也會
  直接貼官方素材（卡圖、收錄內容一覽圖）來源給你核對資料正確性，遇到這種
  情況要老實核對、發現不一致要講清楚，不要因為之前寫過就假設是對的
- 資料庫（`data/cards.js`）的內容屬於「遊戲內容/事實資料」，凡是牽涉到真實
  角色數值、技能文字、張數配置的地方，沒有可靠來源（官方素材、Jolin 提供）
  就不要自己編，寧可標記「不確定」或去查證，也不要用猜的湊上去
- 她原本用 Claude.ai 網頁版聊天做這個專案，後來換到 Claude Code 主要是想用
  Playwright 做視覺測試（Claude.ai 網頁版沙盒環境擋掉了 Playwright 瀏覽器
  執行檔下載）
- 這份文件之前的完整開發過程她自己有聊天記錄，不需要重新交代原始需求脈絡，
  這份文件是濃縮過的現況總結，每次有重大變動就應該整份重新檢視更新，
  過時的段落要刪掉而不是留著疊加
